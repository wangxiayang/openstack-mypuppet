class compute::params {

	$verbose = true
	$package_manage = true
	$controller_hostname = 'controller'
	$controller_ip = '172.16.100.128'
	$service_enable = true
	$service_disable = false
	$service_ensure = 'running'
	$servicestop_ensure = 'stopped'

	# actions for host-specific values, such as ip
}
